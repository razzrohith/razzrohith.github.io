Context:
This is RaithuFresh, a Telangana-first fruits and vegetables marketplace for farmers, buyers, agents, and admins.

Final product goal:
The final goal is to publish RaithuFresh as a real Android app on Google Play Store and a real iOS app on Apple App Store.
For now, complete the web/PWA MVP release-candidate process, keeping everything mobile-app-ready for a future free/open-source Capacitor-style wrapper.

Rules:
- Focus only on fruits and vegetables.
- Use only free tools, free tiers, open-source libraries, and mock/local alternatives.
- Do not use paid services or paid APIs.
- Do not add Farmer Analytics yet.
- Do not add delivery, online payments, Edge Functions, Web Push, VAPID keys, background notifications, paid maps, paid SMS, paid WhatsApp API, paid email, paid analytics, or paid AI.
- Do not use service_role key.
- Do not expose secrets.
- Do not weaken RLS.
- Do not touch DealNest/deals/coupons/stores/threads code or data.
- Use only fake/mock names, phone numbers, emails, villages, and test details during testing.
- Do not use real personal data.
- Keep UI mobile-first and touch-friendly for future Android/iOS packaging.

Current status:
The corrected REVOKE-based RLS patch was manually applied and verified:
- anon PATCH/DELETE farmers is blocked with 401 / 42501.
- anon PATCH/DELETE waitlist_leads is blocked with 401 / 42501.
- anon GET verified farmers still works.
- anon POST waitlist_leads still works.
- Browse Produce and Landing page still work.
- TypeScript is clean.
- No console errors.

Task:
Complete Phase 14 + Phase 15.

Phases:
1. Phase 14 — Pilot Data + Demo Reset
2. Phase 15 — Web Release Candidate QA

Important:
Do not delete real-looking data blindly.
Do not run destructive SQL automatically.
Do not remove seed/demo data required for demo.
If data cleanup requires Supabase action, create a clear SQL file and report it instead of applying risky deletes automatically.

Phase 14 — Pilot Data + Demo Reset

Goal:
Prepare the app for controlled pilot testing without real personal data.

Tasks:

1. Audit current data usage

Check which data appears to be:
- seed/demo farmer data
- fake test users
- fake reservations
- fake waitlist leads
- fake agent requests
- generated test listings

Do not expose or print real personal data in the final report.
Use counts and patterns only.

2. Create pilot-safe data reset plan

Create a clear reset plan for pilot/demo data.

The plan should explain:
- what can be safely kept
- what should be removed before pilot
- what should be manually reviewed
- what tables should not be blindly deleted

3. Create SQL cleanup file, but do not apply destructive cleanup automatically

Create:

supabase/pilot-data-reset.sql

It should include:
- comments explaining each cleanup step
- safe SELECT preview queries
- optional DELETE statements commented out by default
- cleanup examples for fake reservations, fake waitlist leads, fake agent requests, and fake test listings
- instructions to keep verified demo farmers and active produce listings unless intentionally resetting
- rollback/reseed notes if possible

Do not run destructive DELETE automatically.

4. Create pilot test account checklist

Document fake pilot accounts needed:
- fake buyer
- fake farmer
- fake agent
- fake admin

Use fake phone numbers and fake emails only.

5. Create pilot data checklist

Document:
- 5 to 10 pilot farmers
- 20 to 50 pilot buyers
- required farmer fields
- required listing fields
- required privacy checks
- waitlist checks
- agent call request checks

Phase 15 — Web Release Candidate QA

Goal:
Decide whether the web/PWA MVP is ready to freeze before mobile app planning.

Run full QA:

1. Core route QA
Verify:
- /
- /browse
- /produce/:id
- /farmers/:id
- /buyer
- /buyer/reservations/:id
- /farmer
- /agent
- /admin
- /profile
- /login
- /signup

2. Public flow QA
Verify:
- Landing page loads.
- Trust stats load.
- Nearby farmers load.
- Fresh listings load.
- Browse shows only active listings.
- Produce Detail loads live data.
- Farmer Profile loads live data.
- Public pages do not expose buyer phone.
- Waitlist works.

3. Auth/role QA
Verify:
- Signup for buyer/farmer/agent works or is clearly documented if email confirmation blocks automated testing.
- Login/logout works.
- Buyer can access buyer pages only.
- Farmer can access farmer dashboard only.
- Agent can access agent dashboard only.
- Admin can access admin dashboard.
- Wrong roles are blocked.
- User cannot change role from profile.

4. Reservation QA
Verify:
- Guest reservation works.
- Logged-in buyer reservation saves buyer_user_id.
- Buyer dashboard shows own reservations.
- Buyer reservation detail works.
- Buyer can cancel own pending reservation.
- Farmer sees own listing reservations.
- Farmer can confirm/cancel/complete reservations.
- Admin sees all reservations.
- Buyer phone remains private.

5. Farmer listing QA
Verify:
- Farmer can add listing.
- Farmer can edit safe listing fields.
- Farmer can update quantity.
- Farmer can mark Sold.
- Farmer can mark Out of Stock.
- Farmer can reactivate if supported.
- Public Browse only shows active listings.

6. Agent/Admin QA
Verify:
- Agent can create call request.
- Agent can update call request status.
- Admin can view reservations.
- Admin can view agent requests.
- Admin can update statuses.

7. Mobile QA
At 390px width, verify:
- all major pages fit
- no horizontal overflow
- modals scroll
- buttons are touch-friendly
- nav works
- dashboards are readable

8. PWA QA
Verify:
- manifest exists
- PNG icons exist
- maskable icons exist
- apple-touch-icon exists
- service worker exists
- offline page exists
- local dev not broken by service worker
- PWA remains ready for future Android/iOS wrapper

9. Security/cost QA
Verify:
- no service_role key in frontend
- no SUPABASE_DB_URL in frontend
- no secrets printed
- no paid APIs
- no paid services
- no external paid assets
- no hotlinked images
- RLS patch remains verified

10. Build/console QA
Run:
- TypeScript check
- browser console check
- route screenshots/checks

Expected:
- TypeScript zero errors
- no application console errors
- no secrets printed
- no paid services
- no known critical blocker

Documentation:
Update README with:
- Pilot Data + Demo Reset section
- pilot-data-reset.sql usage
- test account checklist
- Web Release Candidate QA summary
- pass/fail criteria
- remaining known limitations
- mobile-app-readiness note
- final recommendation: ready / not ready for mobile planning

Final report format:
Return:
- Phase 14 result
- Phase 15 result
- Files created/changed
- Whether any destructive SQL was applied
- Release candidate status: Ready / Not Ready
- Blockers
- Warnings
- TypeScript result
- Console result
- PWA result
- Mobile result
- Security/cost result
- Recommended next phase