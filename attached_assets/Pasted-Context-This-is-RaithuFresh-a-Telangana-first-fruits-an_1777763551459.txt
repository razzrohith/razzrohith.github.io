Context:
This is RaithuFresh, a Telangana-first fruits and vegetables marketplace for farmers, buyers, agents, and admins.

Rules:
- Focus only on fruits and vegetables.
- Use only free tools, free tiers, open-source libraries, and mock/local alternatives.
- Do not use paid services or paid APIs.
- Do not use paid stock images.
- Do not use copyrighted brand images.
- Do not hotlink images from random websites.
- Do not touch DealNest/deals/coupons/stores/threads code or data.
- Do not add map, delivery, payments, admin promotion tools, Edge Functions, or paid services.
- Do not use service_role key.
- Do not expose secrets.
- Do not weaken RLS.
- Use only fake/mock names, phone numbers, emails, villages, and test details during testing.
- Do not use real personal data.

Visual assets / UI realism rule:
Where useful, use existing local SVG assets or create small self-made local SVG assets only.
Store assets locally in public/assets if new assets are needed.
Add alt text wherever images are rendered.
Do not use external image hotlinks.

Task:
Run a Buyer Dashboard Reservation Management + UX Polish phase.

Current state:
- Buyer Dashboard exists at /buyer.
- /buyer is protected for buyer and admin roles.
- reservations.buyer_user_id links logged-in buyer reservations to auth.users(id).
- Guest reservations still work with buyer_user_id = null.
- Buyer Dashboard shows buyer’s own reservations.
- Buyer cannot update reservation status currently.
- Farmer/admin reservation access still works.
- No paid services are used.

Goal:
Make Buyer Dashboard more useful while keeping reservation privacy and RLS safe.

Tasks:

1. Add reservation status filters

On Buyer Dashboard, add filter tabs or chips:

- All
- Pending
- Confirmed
- Completed
- Cancelled

Show count badge on each tab.

Default: All.

2. Add search inside Buyer Dashboard

Add client-side search for buyer’s own reservation list.

Search should match:
- Produce name
- Farmer name
- Village
- District
- Status

No paid search service.
No server-side search needed.

3. Add buyer cancellation for pending reservations only

Allow logged-in buyer to cancel their own reservation only when:

- reservation.buyer_user_id = auth.uid()
- current reservation status = pending

Button:
"Cancel Request"

Confirmation:
Use a simple browser confirm or existing dialog component if available.

Expected behavior:
- Buyer can cancel only their own pending reservations.
- Buyer cannot cancel confirmed/completed/cancelled reservations.
- Buyer cannot update reservation to confirmed/completed.
- Buyer cannot update another buyer’s reservation.
- Buyer cannot edit buyer_name, buyer_phone, listing_id, quantity_kg, payment_method, or created_at.

Database/RLS:
Create patch if needed:

supabase/patch-buyer-cancel-reservation.sql

Add only the minimum safe policy.

Suggested policy:
- UPDATE status only
- TO authenticated
- USING buyer_user_id = auth.uid() AND status = 'pending'
- WITH CHECK buyer_user_id = auth.uid() AND status = 'cancelled'

Do not weaken farmer/admin policies.
Do not allow buyer broad UPDATE.
Do not allow buyer DELETE.

4. Update Supabase helper

Add helper if needed:

cancelBuyerReservation(reservationId)

It should:
- update status to cancelled
- rely on RLS for ownership and pending-only protection
- return success/failure clearly

5. Improve Buyer Dashboard reservation cards

Improve visual clarity.

Each reservation card should show:
- Produce name
- Category icon
- Farmer name
- Village/district
- Quantity
- Estimated total
- Status badge
- Reserved date
- Payment method
- Pickup location
- View Listing button
- Contact Farmer button
- Cancel Request button only when status = pending

Use existing local SVG assets:
- icon-fruit.svg
- icon-vegetable.svg
- empty-produce.svg

No external images.

6. Add better empty states

If no reservations at all:
Show:
"No reservations yet. Browse fresh produce and reserve directly from nearby farmers."

Button:
"Browse Produce"

If filters/search return no results:
Show:
"No reservations match your filters."

Button:
"Clear filters"

7. Keep existing behavior

Do not break:
- Guest reservation flow
- Logged-in buyer reservation insert with buyer_user_id
- Farmer Dashboard reservation visibility
- Admin Dashboard reservation visibility
- Browse Produce
- Produce Detail
- Farmer Profile
- Contact Farmer dialog
- WhatsApp/tel/copy
- Waitlist form
- PWA files

8. Privacy/security

Verify:
- Buyer sees only own reservations.
- Buyer cannot see another buyer’s reservations.
- Buyer can cancel only own pending reservations.
- Buyer cannot update confirmed/completed/cancelled reservations.
- Buyer cannot update protected reservation fields.
- Buyer cannot delete reservations.
- Farmer can still manage reservations for own listings.
- Admin can still manage all reservations.
- Guest users cannot read reservations.
- Buyer phone is not public.
- No secrets printed.
- No RLS weakening.

9. Testing

Use fake/mock data only.

Fake buyer:
- Name: Ravi Test Buyer
- Phone: 9876500001
- Email: fakebuyer@example.com

Test:
- Buyer Dashboard loads only current buyer’s reservations.
- Status tabs work.
- Search works.
- Clear filters works.
- Pending count is correct.
- Confirmed count is correct.
- Completed count is correct.
- Cancelled count is correct.
- Buyer can cancel own pending reservation.
- Cancelled status persists in Supabase.
- Cancel Request button disappears after cancellation.
- Buyer cannot cancel another buyer’s reservation.
- Buyer cannot cancel confirmed/completed reservation.
- Buyer cannot update buyer_phone or buyer_name.
- Farmer Dashboard still sees reservation status correctly.
- Admin Dashboard still sees reservation status correctly.
- Guest reservation still works.
- Logged-in buyer reservation still saves buyer_user_id.
- Browse Produce still works.
- Produce Detail still works.
- Farmer Profile still works.
- Agent Dashboard still works.
- Admin Dashboard still works.
- Waitlist form still works.
- PWA files still exist.
- TypeScript has zero errors.
- No console errors.
- No secrets printed.
- No paid services used.

10. Documentation

Update README with:
- Buyer Dashboard filters
- Buyer Dashboard search
- Buyer cancellation rules
- RLS protection for buyer cancellation
- Guest vs logged-in reservation behavior
- Privacy rules
- Fake/mock testing rule
- No paid services used

After completion, report:
- Files changed
- SQL patch added and run, if needed
- RLS policies added/changed
- Buyer Dashboard filters added
- Buyer Dashboard search added
- Whether buyer cancellation works
- Whether buyer cancellation is blocked for non-pending reservations
- Whether buyer cannot update protected fields
- Whether farmer/admin reservation flows still work
- TypeScript result
- Console errors, if any
- Remaining Buyer Dashboard limitations