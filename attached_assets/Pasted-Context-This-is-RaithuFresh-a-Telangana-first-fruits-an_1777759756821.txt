Context:
This is RaithuFresh, a Telangana-first fruits and vegetables marketplace for farmers, buyers, agents, and admins.

Rules:
- Focus only on fruits and vegetables.
- Use only free tools, free tiers, open-source libraries, and mock/local alternatives.
- Do not use paid services or paid APIs.
- Do not touch DealNest/deals/coupons/stores/threads code or data.
- Do not add map, delivery, payments, admin promotion tools, Edge Functions, or paid services.
- Do not use service_role key.
- Do not expose secrets.
- Do not weaken RLS.
- Use only fake/mock names, phone numbers, emails, villages, and test details during testing.
- Do not use real personal data.

Task:
Run a Buyer Sharing + Detail UX phase.

Current state:
- Browse Produce loads live Supabase listings.
- Produce Detail loads live Supabase listing data.
- Contact Farmer dialog supports tap-to-call, copy phone, and free WhatsApp wa.me link.
- Reservation flow works from Browse and Detail.
- Buyer phone is not public.

Goal:
Improve the buyer-facing listing detail and sharing experience without adding paid services or large new systems.

Tasks:

1. Add Share Listing button on Produce Detail

Add a visible button:

"Share Listing"

Behavior:
- Use the free browser Web Share API when available.
- If Web Share API is not available, copy the current listing URL to clipboard.
- If clipboard fails, show a friendly fallback message with the URL.
- Do not use any paid sharing API.
- Do not require login.

Share content should include:
- Produce name
- Price per kg
- Village/district
- RaithuFresh listing URL

Example:
"Fresh Mango (Banaganapalli) available on RaithuFresh for ₹80/kg near Shadnagar. View listing: [URL]"

2. Add Share button on Browse Produce cards

Add a small "Share" action on each listing card.

Behavior:
- Share/copy the specific listing detail URL.
- Use the same helper logic as Produce Detail.
- Keep the card clean; do not redesign the full page.

3. Improve Produce Detail trust/clarity section

On Produce Detail, add a small "Before Pickup" or "Buyer Notes" section with simple points:

- Contact the farmer before pickup.
- Confirm available quantity before traveling.
- Payment is Cash or UPI directly to the farmer.
- RaithuFresh does not handle online payment in this MVP.

Keep language simple.

4. Reservation safety improvements

On Produce Detail:
- Disable or clearly warn if listing status is not active.
- Prevent reservation quantity from exceeding available quantity.
- Keep existing fake/mock testing behavior.
- Do not add online payment.

On Browse:
- If a listing is not active, do not allow reservation.
- Since Browse currently shows active listings, this should not affect normal flow.

5. Similar produce suggestions on Detail page

Add a small "Similar nearby produce" section below the detail content.

Use free Supabase query only.

Show up to 3 listings with:
- Same category OR same district
- Exclude current listing
- Status = active
- Produce name
- Price per kg
- Village/district
- View Details button

If Supabase fails or no similar listings exist, show nothing or a simple empty state.
Do not add recommendations AI.
Do not use paid APIs.

6. Privacy and security

Verify:
- Buyer phone is not shown on public pages.
- Farmer phone is allowed only in Contact Farmer dialog.
- Reservation buyer phone remains visible only to listing owner farmer and admin.
- No RLS changes unless absolutely required.
- No secrets printed.

7. Regression testing

Use fake/mock data only.

Test:
- Browse Produce loads 15 live Supabase listings.
- Share button works from Browse.
- Share button works from Produce Detail.
- Web Share API fallback to clipboard works.
- Contact Farmer dialog still works.
- WhatsApp button still works.
- Tap-to-call still works.
- Copy phone still works.
- Reservation from Browse still works.
- Reservation from Detail still works.
- Similar listings load if available.
- Invalid listing ID still shows not-found state.
- Farmer Dashboard still works.
- Agent Dashboard still works.
- Admin Dashboard still works.
- Waitlist form still works.
- PWA files still exist.
- TypeScript has zero errors.
- No console errors.
- No secrets printed.
- No paid services used.

8. Documentation

Update README with:
- Share Listing behavior
- Web Share API + clipboard fallback
- Similar produce section
- Buyer pickup notes
- No online payment
- Buyer phone privacy rule
- Fake/mock testing rule
- No paid services used

After completion, report:
- Files changed
- Share Listing behavior on Detail
- Share Listing behavior on Browse
- Whether clipboard fallback works
- Whether reservation safety improvements work
- Whether similar listings load
- TypeScript result
- Console errors, if any
- Any remaining buyer-flow limitations