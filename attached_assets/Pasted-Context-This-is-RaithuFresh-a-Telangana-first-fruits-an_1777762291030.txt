Context:
This is RaithuFresh, a Telangana-first fruits and vegetables marketplace for farmers, buyers, agents, and admins.

Rules:
- Focus only on fruits and vegetables.
- Use only free tools, free tiers, open-source libraries, and mock/local alternatives.
- Do not use paid services or paid APIs.
- Do not use paid stock images.
- Do not use copyrighted brand images.
- Do not hotlink images from random websites.
- Do not touch DealNest/deals/coupons/stores/threads code or data.
- Do not add map, delivery, payments, admin promotion tools, Edge Functions, or paid services.
- Do not use service_role key.
- Do not expose secrets.
- Do not weaken RLS.
- Use only fake/mock names, phone numbers, emails, villages, and test details during testing.
- Do not use real personal data.

Visual assets / UI realism rule:
Where useful, use existing local SVG assets or create small self-made local SVG assets only.
Store assets locally in public/assets if new assets are needed.
Add alt text wherever images are rendered.
Do not use external image hotlinks.

Task:
Run a Landing Trust Indicators + Live Stats phase.

Current state:
- Landing page has polished hero with local SVG assets.
- Farmer of the Week section works with live Supabase data.
- Nearby Verified Farmers loads live Supabase farmers.
- Fresh Listings Near You loads live Supabase listings.
- Waitlist form works.
- No paid services are used.

Goal:
Add a clean trust/stats strip to the landing page using live Supabase data, without schema changes.

Tasks:

1. Add live trust indicators strip

Add a thin section between the Hero and “How the Pilot Works”.

Show 3 to 5 stat cards:

- Verified farmers
- Active listings
- Districts covered
- Fresh fruits listed
- Fresh vegetables listed

Use live Supabase data if configured.

If Supabase fails:
- Show safe fallback values or hide the numbers gracefully.
- Do not crash.

2. Add Supabase helper if needed

In src/lib/supabase.ts, add a helper if useful:

getLandingStats()

It should calculate:
- verified farmer count
- active listing count
- unique district count
- fruit listing count
- vegetable listing count

Use existing public-safe data only.
Do not expose phone numbers.
Do not expose reservations.
Do not expose admin data.
Do not weaken RLS.

3. Improve landing trust wording

Add simple text near the stats:

“Built for local pickup. Farmers list fresh fruits and vegetables, buyers reserve and contact directly.”

Keep language simple.

4. Use local visuals only

Use existing icons/assets where helpful:
- icon-fruit.svg
- icon-vegetable.svg
- icon-farmer-week.svg
or Lucide icons already installed.

Do not add paid images.
Do not use external images.

5. Keep existing landing behavior

Do not break:
- Join as Buyer CTA
- Join as Farmer CTA
- Browse Produce CTA
- Farmer of the Week
- Nearby Verified Farmers
- Fresh Listings Near You
- Waitlist form validation
- Waitlist Supabase insert
- Double-submit prevention
- Mobile layout

6. Testing

Use fake/mock data only.

Test:
- Landing page loads.
- Trust strip appears between hero and pilot steps.
- Stats load from Supabase.
- Verified farmers count is correct or safely calculated.
- Active listings count is correct or safely calculated.
- District count is correct or safely calculated.
- Fruit/Vegetable counts are correct or safely calculated.
- If Supabase fails, page does not crash.
- All CTAs still work.
- Waitlist submit still works.
- Browse Produce still works.
- Produce Detail still works.
- Farmer Profile still works.
- Farmer Dashboard still works.
- Agent Dashboard still works.
- Admin Dashboard still works.
- PWA files still exist.
- TypeScript has zero errors.
- No console errors.
- No secrets printed.
- No paid services used.

7. Documentation

Update README with:
- Trust Indicators strip
- getLandingStats helper if added
- What public-safe stats are shown
- No private data exposed
- Fake/mock testing rule
- No paid services used

After completion, report:
- Files changed
- Supabase helper added/updated
- Which stats are shown
- Whether stats load from Supabase
- Whether landing CTAs and waitlist still work
- TypeScript result
- Console errors, if any
- Any remaining landing page limitations