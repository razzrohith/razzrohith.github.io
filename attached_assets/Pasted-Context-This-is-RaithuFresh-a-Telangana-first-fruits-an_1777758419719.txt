Context:
This is RaithuFresh, a Telangana-first fruits and vegetables marketplace for farmers, buyers, agents, and admins.

Rules:
- Focus only on fruits and vegetables.
- Use only free tools, free tiers, open-source libraries, and mock/local alternatives.
- Do not use paid services or paid APIs.
- Do not touch DealNest/deals/coupons/stores/threads code or data.
- Do not add map, delivery, payments, admin promotion tools, Edge Functions, or new UI features.
- Do not use service_role key.
- Do not expose secrets.
- Do not weaken RLS.
- Use only fake/mock names, phone numbers, emails, villages, and test details during testing.
- Do not use real personal data.

Task:
Apply the final small cleanup from the latest security report.

Current remaining informational issue:
authenticated still has table-level DELETE grant on reservations, but no DELETE RLS policy exists, so RLS blocks it.

Goal:
Remove this unnecessary DELETE grant.

Step 1 — Create SQL patch

Create:

supabase/patch-reservation-delete-cleanup.sql

Apply safely:

REVOKE DELETE ON reservations FROM authenticated;
REVOKE DELETE ON reservations FROM anon;

Do not add any DELETE policy.
Do not change reservation INSERT, SELECT, or UPDATE policies.
Do not change frontend UI.

Step 2 — Verify

Verify:
- authenticated has no DELETE grant on reservations.
- anon has no DELETE grant on reservations.
- buyers can still create reservations.
- farmers can still read their own reservations.
- farmers can still update reservation status.
- admins can still read all reservations.
- admins can still update reservation status.
- Browse Produce still works.
- Farmer Dashboard still works.
- Admin Dashboard still works.
- TypeScript has zero errors.
- No console errors.
- No secrets printed.

Step 3 — Documentation

Update README briefly:
- reservations DELETE grant removed from anon/authenticated
- no user role can delete reservations in MVP
- reservation status changes are used instead of deletion
- no paid services are used
- testing uses fake/mock data only

After completion, report:
- Files changed
- SQL patch added and run
- Grants revoked
- Whether reservation create/read/status-update still works
- TypeScript result
- Console errors if any
- Remaining security tradeoffs