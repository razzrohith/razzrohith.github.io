Context:
This is RaithuFresh, a Telangana-first fruits and vegetables marketplace for farmers, buyers, agents, and admins.

Rules:
- Focus only on fruits and vegetables.
- Use only free tools, free tiers, open-source libraries, and mock/local alternatives.
- Do not use paid services or paid APIs.
- Do not use paid stock images.
- Do not use copyrighted brand images.
- Do not hotlink images from random websites.
- Do not touch DealNest/deals/coupons/stores/threads code or data.
- Do not add delivery, online payments, admin promotion tools, Edge Functions, or paid services.
- Do not use service_role key.
- Do not expose secrets.
- Do not weaken RLS.
- Use only fake/mock names, phone numbers, emails, villages, and test details during testing.
- Do not use real personal data.

Visual assets / UI realism rule:
Use existing local SVG assets where useful:
- empty-produce.svg
- icon-fruit.svg
- icon-vegetable.svg
- icon-farmer-week.svg

Do not use external images.
Do not add paid assets.

Task:
Run a Farmer Dashboard Realtime + Listing Management Polish phase.

Current state:
- Farmer Dashboard is protected for farmer/admin roles.
- Logged-in farmer is linked to farmers.user_id.
- Farmer listings are scoped to the logged-in farmer.
- Farmer can create listings.
- Farmer can mark listing Sold / Out of Stock.
- Farmer can view reservations for their own listings.
- Farmer can update reservation status.
- New pending reservation badge/banner exists.
- “Mark all as seen” clears banner/chip/navbar badge without reload.

Goal:
Make Farmer Dashboard more useful for real pilot operations.

Tasks:

1. Realtime reservation updates

Add Supabase Realtime subscription on Farmer Dashboard if available on the free tier.

Behavior:
- Subscribe to reservation changes relevant to the farmer’s listings.
- When a new reservation arrives:
  - Refresh reservations.
  - Update pending count.
  - Show amber banner/chip if it is a new pending reservation.
  - Update Navbar badge using the existing custom event system.
- When reservation status changes:
  - Refresh reservation list.
  - Keep UI consistent.

Important:
- Use Supabase built-in realtime only if available in the current free project.
- Do not enable any paid Supabase feature.
- Do not require paid add-ons.
- If Realtime requires extra setup that may be paid or unavailable, do not force it.
- Add a safe fallback manual refresh button.

2. Manual refresh fallback

Add a clear “Refresh reservations” button in Farmer Dashboard.

It should:
- Reload reservations.
- Reload listing data if needed.
- Not reload the whole page.
- Show loading state while refreshing.

3. Listing edit modal/form

Add ability for farmer to edit their own listing safe fields:

Allowed editable fields:
- Produce name
- Category
- Quantity kg
- Price per kg
- Harvest date/time
- Pickup location
- District
- Quality notes
- Status

Do not allow editing:
- id
- farmer_id
- created_at
- user_id
- any other farmer’s listing

Use existing RLS and column-level grants.
Do not weaken RLS.

4. Quick quantity update

Add quick quantity update action on each farmer listing card/table.

Example:
- Update quantity available
- Save
- Show success/error message

Use fake/mock values for testing.

5. Better listing status actions

Improve status buttons:

For active listing:
- Mark Sold
- Mark Out of Stock

For sold/out_of_stock listing:
- Allow Reactivate only if safe and supported by current status domain.
- If Reactivate is not safe, do not add it.

Status labels should be consistent:
- active
- sold
- out_of_stock
- reserved if used

Do not create invalid statuses.

6. Reservation cards polish

Improve Farmer Dashboard reservation cards.

Each reservation should show:
- Buyer name
- Buyer phone
- Produce name
- Quantity reserved
- Status badge
- Payment method
- Created date
- Pickup location
- Action buttons based on status:
  - Pending → Confirm / Cancel
  - Confirmed → Complete / Cancel
  - Completed / Cancelled → no action buttons

Keep buyer phone visible only here because this farmer owns the listing.

7. Filters for farmer reservations

Add simple filters:
- All
- Pending
- Confirmed
- Completed
- Cancelled

Add counts per status.

8. Empty/loading/error states

Improve:
- No listings yet
- No reservations yet
- No pending reservations
- Realtime disconnected or unavailable
- Failed to update listing
- Failed to update reservation

Use simple wording.

9. Privacy/security

Verify:
- Farmer sees only own listings.
- Farmer sees only reservations for own listings.
- Farmer cannot edit another farmer’s listing.
- Farmer cannot change farmer_id.
- Farmer cannot edit protected listing columns.
- Farmer cannot see another farmer’s buyer phone.
- Buyer Dashboard still only shows buyer’s own reservations.
- Admin Dashboard still works.
- No RLS weakening.
- No secrets printed.
- No paid services used.

10. Testing

Use fake/mock data only.

Test:
- Farmer Dashboard loads for farmer user.
- Listings load for current farmer only.
- Add listing works.
- Edit listing safe fields works.
- Quick quantity update works.
- Mark Sold works.
- Mark Out of Stock works.
- Reactivate works only if implemented safely.
- Reservation list loads.
- Reservation filters work.
- Pending count is correct.
- Mark all as seen still works.
- Navbar badge still updates without reload.
- Manual refresh works.
- Realtime update works if available.
- If Realtime is unavailable, fallback works cleanly.
- Farmer cannot edit another farmer’s listing.
- Farmer cannot see another farmer’s reservations.
- Browse Produce still works.
- Produce Detail still works.
- Farmer Profile still works.
- Buyer Dashboard still works.
- Buyer Reservation Detail still works.
- Agent Dashboard still works.
- Admin Dashboard still works.
- Waitlist form still works.
- PWA files still exist.
- TypeScript has zero errors.
- No console errors.
- No secrets printed.
- No paid services used.

11. Documentation

Update README with:
- Farmer Dashboard realtime behavior
- Manual refresh fallback
- Listing edit behavior
- Quick quantity update
- Reservation filters
- Mark all as seen behavior
- Privacy/security rules
- Fake/mock testing rule
- No paid services used

After completion, report:
- Files changed
- Whether Realtime was added
- Whether fallback refresh works
- Whether listing edit works
- Whether quick quantity update works
- Whether reservation filters work
- Whether Mark all as seen still works
- Whether RLS blocked unsafe edits correctly
- TypeScript result
- Console errors, if any
- Remaining Farmer Dashboard limitations