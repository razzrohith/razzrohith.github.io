Context:
This is RaithuFresh, a Telangana-first fruits and vegetables marketplace for farmers, buyers, agents, and admins.

Rules:
- Focus only on fruits and vegetables.
- Use only free tools, free tiers, open-source libraries, and mock/local alternatives.
- Do not use paid services or paid APIs.
- Do not touch DealNest/deals/coupons/stores/threads code or data.
- Do not add map, delivery, payments, admin promotion tools, Edge Functions, or new major features.
- Do not use service_role key.
- Do not expose secrets.
- Do not weaken RLS.
- Use only fake/mock names, phone numbers, emails, villages, and test details during testing.
- Do not use real personal data.

Task:
Run a Buyer Flow Cleanup phase.

Current state:
- Browse Produce loads 15 real Supabase listings.
- Produce Detail now loads real Supabase listing data.
- Reservation from Produce Detail uses real listing id.
- Contact Farmer works on Produce Detail.
- BrowsePage Contact button still uses mockFarmers instead of real Supabase farmerMap.

Goal:
Make the buyer browsing and contact flow consistent across Browse Produce and Produce Detail.

Tasks:

1. Fix BrowsePage Contact Farmer button

Update BrowsePage so the Contact Farmer button uses real Supabase farmer data from the farmerMap/listing join.

Expected behavior:
- If farmer phone exists, clicking Contact Farmer should show/call/display the real farmer phone.
- If farmer phone is missing, show:
  "Please reserve first or visit the pickup location."
- Do not use mockFarmers for live Supabase listings.
- Keep mock fallback only when Supabase is not configured.

2. Keep buyer phone privacy

Make sure buyer phone numbers are never shown publicly.

Allowed:
- Farmer phone may be shown/contacted as part of produce listing contact flow.
- Buyer phone should only be visible to:
  - the farmer who owns the listing
  - admin users

3. Verify Browse → Detail → Reserve flow

Test:
- Browse Produce loads 15 Supabase listings.
- Contact Farmer works from Browse Produce for live Supabase listings.
- View Full Details opens the correct Supabase listing id.
- Produce Detail shows the same listing data as Browse.
- Reservation from Produce Detail inserts into reservations table using real listing_id.
- Farmer Dashboard shows that reservation under the correct farmer.
- Invalid listing id still shows not-found state.

4. Improve small buyer UX only if needed

Only make small safe improvements if clearly needed:
- Better loading message
- Better error message
- Better empty state
- Clearer reservation success message

Do not redesign the page.

5. Regression test

Test:
- Landing page works.
- Browse Produce works.
- Produce Detail works.
- Reservation flow works.
- Farmer Dashboard works.
- Agent Dashboard works.
- Admin Dashboard works.
- Waitlist form works.
- PWA files still exist.
- TypeScript has zero errors.
- No console errors.
- No secrets printed.
- No paid services used.

6. Documentation

Update README with:
- Browse Contact now uses live Supabase farmer phone when available.
- Produce Detail uses live Supabase listing data.
- Buyer phone privacy rule.
- Testing uses fake/mock data only.
- No paid services are used.

After completion, report:
- Files changed
- What was fixed in BrowsePage Contact Farmer
- Whether Browse → Detail routing works
- Whether reservation from Detail works
- Whether Farmer Dashboard receives the reservation
- TypeScript result
- Console errors, if any
- Any remaining buyer-flow limitations