Context:
This is RaithuFresh, a Telangana-first fruits and vegetables marketplace for farmers, buyers, agents, and admins.

Rules:
- Focus only on fruits and vegetables.
- Use only free tools, free tiers, open-source libraries, and mock/local alternatives.
- Do not use any paid service, paid API, paid Supabase add-on, paid Replit feature, paid SMS/WhatsApp API, paid email API, paid map API, paid analytics, or paid AI API.
- Do not touch DealNest/deals/coupons/stores/threads code or data.
- Do not add map, delivery, payments, admin promotion tools, Edge Functions, or new UI features.
- Do not use service_role key.
- Do not expose secrets.
- Do not weaken RLS.
- Use only fake/mock names, phone numbers, emails, villages, and test details during testing.
- Do not use real personal data.

Task:
Apply one more small post-QA security hardening patch for produce_listings.

Do not change frontend UI unless the patch breaks an existing function.
Do not add new features.

Current remaining MVP tradeoffs:
1. anon still has table-level UPDATE and DELETE grants on produce_listings, even though RLS blocks them.
2. farmers can update any column on their own listing.

Goal:
Tighten produce_listings permissions without breaking the Farmer Dashboard.

Step 1 — Create SQL patch

Create:

supabase/patch-produce-listing-update-security.sql

Required changes:

A. Remove unused anon write grants

Run safely/idempotently:

- REVOKE UPDATE ON produce_listings FROM anon
- REVOKE DELETE ON produce_listings FROM anon

Keep anon SELECT for active listings:
- Public users must still be able to browse active produce listings.

B. Restrict authenticated UPDATE columns

Remove broad authenticated UPDATE if present:

- REVOKE UPDATE ON produce_listings FROM authenticated

Then grant UPDATE only on safe editable columns:

Allowed farmer-editable columns:
- produce_name
- category
- quantity_kg
- price_per_kg
- harvest_datetime
- pickup_location
- district
- distance_km
- quality_notes
- status
- updated_at

Do NOT allow farmers to update:
- id
- farmer_id
- created_at

Use column-level GRANT:

GRANT UPDATE (
  produce_name,
  category,
  quantity_kg,
  price_per_kg,
  harvest_datetime,
  pickup_location,
  district,
  distance_km,
  quality_notes,
  status,
  updated_at
) ON produce_listings TO authenticated;

C. Keep existing RLS ownership policy

Keep farmer_update_own_listing policy or recreate it safely so:
- authenticated farmers can update only listings where produce_listings.farmer_id belongs to their own farmers row
- farmers cannot update another farmer’s listing
- farmers cannot move a listing to another farmer by changing farmer_id

D. Keep existing insert policy

Do not break:
- farmer_insert_own_listing

New listing creation from Farmer Dashboard must still work.

Step 2 — Verify DB permissions

Verify:
- anon cannot INSERT produce_listings
- anon cannot UPDATE produce_listings
- anon cannot DELETE produce_listings
- anon can still SELECT active produce_listings
- authenticated farmer cannot update id
- authenticated farmer cannot update farmer_id
- authenticated farmer cannot update created_at
- authenticated farmer can update allowed fields on own listing
- authenticated farmer cannot update another farmer’s listing

Step 3 — Test frontend

Use fake/mock data only.

Test:
1. Browse Produce still loads active listings publicly.
2. Farmer Dashboard still loads for farmer user.
3. Farmer Dashboard can create a fake fruit/vegetable listing.
4. Farmer Dashboard can update listing status to Sold.
5. Farmer Dashboard can update listing status to Out of Stock.
6. If edit listing exists, editing safe fields still works.
7. Farmer cannot change farmer_id.
8. Farmer cannot edit another farmer’s listing.
9. Admin Dashboard still loads.
10. Agent Dashboard still loads.
11. Reservation flow still works.
12. Waitlist form still works.
13. TypeScript has zero errors.
14. No console errors.
15. No secrets printed.

Step 4 — Documentation

Update README with:
- produce_listings anon write access removed
- farmers can update only safe listing fields
- protected fields: id, farmer_id, created_at
- testing uses fake/mock data only
- no paid services are used

After completion, report:
- Files changed
- SQL patch added and run
- Grants revoked
- Column-level grants added
- Policies kept/changed
- Whether public Browse Produce still works
- Whether Farmer Dashboard listing create/update still works
- Whether farmer_id update is blocked
- Whether anon write access is blocked
- TypeScript result
- Console errors, if any
- Remaining security tradeoffs