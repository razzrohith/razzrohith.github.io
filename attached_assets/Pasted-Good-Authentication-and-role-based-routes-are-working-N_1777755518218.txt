Good. Authentication and role-based routes are working.

Next, link logged-in farmer users to their real farmers table row.

Important:
Do not add map.
Do not add delivery.
Do not add payments.
Do not add admin reservations tab yet.
Do not rebuild the UI.
Do not remove existing auth, PWA, Supabase, or dashboard functionality.
Do not touch DealNest-style data.
Keep the project focused only on fruits and vegetables.

Current issue:
Farmer Dashboard is protected by role, but produce listings still use a hardcoded demo farmer UUID. Fix this so each logged-in farmer saves and views their own listings.

Goal:
When a user signs up or logs in as role = farmer:
- They should have a linked row in the farmers table.
- Farmer Dashboard should use that farmer row.
- New produce listings should save with that farmer_id.
- Farmer Dashboard should show only that farmer’s own listings.
- If the farmer row does not exist yet, create it from user_profiles.

Step 1 — Database patch

Create a new SQL patch file:

supabase/patch-farmer-auth-link.sql

Add a nullable auth link column to farmers:

- user_id UUID references auth.users(id) on delete cascade
- unique(user_id)

Do this safely:
- Use ALTER TABLE only if the column does not already exist.
- Add index if useful.
- Do not break existing seed farmers.
- Existing seed farmers can keep user_id as null.

RLS requirements:
Keep public read for verified farmers if already present.

Add authenticated policies so:
- A farmer can read their own farmers row where farmers.user_id = auth.uid()
- A farmer can insert their own farmers row with user_id = auth.uid()
- A farmer can update their own farmers row where user_id = auth.uid()
- A farmer can insert produce_listings only for their own farmer_id
- A farmer can update only produce_listings connected to their own farmer row
- A farmer can read their own listings, including sold/out_of_stock/reserved listings
- Public users can still read active produce listings for Browse Produce

If admin role logic is difficult in SQL, keep admin access unchanged for now and document it. Do not weaken security broadly.

Step 2 — Update Supabase types/helpers

Update Supabase helper/types so farmers include:
- user_id

Add helper functions if useful:
- getOrCreateFarmerForCurrentUser()
- getFarmerByCurrentUser()
- getFarmerListings()
- createFarmerListing()

Use the logged-in user and user_profiles data:
- full_name → farmers.name
- phone → farmers.phone
- village → farmers.village
- district → farmers.district if available
- assisted_mode default false
- verified default false unless existing project requires true for testing

Step 3 — Signup flow

When a user signs up with role = farmer:
- Create user_profiles row
- Also create a farmers row linked to auth user id if possible

If email confirmation blocks immediate session access, handle this safely:
- Do not crash
- Create farmer row later when they first open Farmer Dashboard after login

Step 4 — Farmer Dashboard

Remove hardcoded demo farmer usage for logged-in farmers.

Behavior:
- If logged-in farmer has linked farmers row, use it.
- If logged-in farmer does not have farmers row, create one from user_profiles.
- Show farmer name/village from linked farmer/profile.
- Load only produce_listings where farmer_id = current farmer.id.
- Add new listings using current farmer.id.
- Update listing status only for current farmer’s own listings.
- Keep demo/mock fallback only when Supabase/auth is unavailable.

Preserve existing Farmer Dashboard features:
- Profile card
- Add listing form
- Current listings
- Edit listing
- Mark sold
- Mark out of stock
- Buyer interest section

Step 5 — Testing

Test:
1. Farmer signup creates user_profiles row.
2. Farmer login works.
3. Farmer Dashboard creates or finds linked farmers row.
4. New listing saves with the logged-in farmer’s farmer_id, not demo farmer UUID.
5. Farmer Dashboard shows only that farmer’s own listings.
6. Mark Sold / Out of Stock works only on that farmer’s listings.
7. Browse Produce still shows active Supabase listings.
8. Public users can still browse produce.
9. Agent Dashboard still works.
10. Admin Dashboard still works.
11. Waitlist form still works.
12. Reservation flow still works.
13. No console errors.
14. No secrets printed.

Step 6 — Documentation

Update README with:
- How farmer auth linking works
- How farmers table user_id works
- What happens if farmer row does not exist
- How to test with a farmer signup
- Current MVP limitations

Security note:
Do not expose service role key.
Do not expose SUPABASE_DB_URL in frontend.
Do not allow public users to edit farmer listings.
Do not allow farmers to edit other farmers’ listings.

After completion, report:
- Files changed
- SQL patch added and run
- RLS policies added/changed
- Whether farmer signup creates/links farmers row
- Whether Farmer Dashboard no longer uses demo farmer UUID for logged-in farmers
- Exact tests run
- Any RLS issues or limitations