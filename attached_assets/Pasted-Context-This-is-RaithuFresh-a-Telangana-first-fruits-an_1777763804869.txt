Context:
This is RaithuFresh, a Telangana-first fruits and vegetables marketplace for farmers, buyers, agents, and admins.

Rules:
- Focus only on fruits and vegetables.
- Use only free tools, free tiers, open-source libraries, and mock/local alternatives.
- Do not use paid services or paid APIs.
- Do not use paid stock images.
- Do not use copyrighted brand images.
- Do not hotlink images from random websites.
- Do not touch DealNest/deals/coupons/stores/threads code or data.
- Do not add delivery, online payments, admin promotion tools, Edge Functions, or paid services.
- Do not use service_role key.
- Do not expose secrets.
- Do not weaken RLS.
- Use only fake/mock names, phone numbers, emails, villages, and test details during testing.
- Do not use real personal data.

Visual assets / UI realism rule:
Use existing local SVG assets where useful:
- empty-produce.svg
- icon-fruit.svg
- icon-vegetable.svg
- icon-farmer-week.svg

Do not use external images.
Do not add paid assets.

Task:
Run a Buyer Reservation Detail + Dashboard Flow phase.

Current state:
- Buyer Dashboard exists at /buyer.
- Buyer Dashboard shows only the logged-in buyer’s reservations.
- Buyer can filter/search reservations.
- Buyer can cancel only their own pending reservations.
- Guest reservations still work.
- Farmer/admin reservation access still works.
- Reservation status updates are protected by RLS.

Goal:
Add a focused reservation detail page for logged-in buyers and improve the dashboard-to-detail flow.

Tasks:

1. Add Buyer Reservation Detail route

Create protected route:

/buyer/reservations/:id

Allowed roles:
- buyer
- admin

This page should show one reservation in detail.

Access rules:
- Buyer can view only their own reservation where buyer_user_id = auth.uid().
- Admin can view if current RLS/helper already permits it.
- Do not expose reservations to guests.
- Do not expose one buyer’s reservation to another buyer.

2. Supabase helper

In src/lib/supabase.ts, add helper:

getBuyerReservationById(reservationId)

It should return reservation details with:
- reservation id
- status
- quantity_kg
- payment_method
- created_at
- produce listing:
  - produce_name
  - category
  - price_per_kg
  - harvest_datetime
  - pickup_location
  - district
  - status
- farmer:
  - name
  - village
  - district
  - rating
  - verified
  - phone if needed for Contact Farmer dialog

Use existing RLS.
Do not weaken RLS.

3. Reservation Detail page content

Show:
- Produce name
- Reservation status badge
- Quantity reserved
- Estimated total
- Payment method
- Reserved date
- Harvest date/time
- Pickup location
- Farmer name
- Farmer village/district
- Farmer verified badge if available
- Farmer rating if available

Add buttons:
- View Listing
- View Farmer Profile
- Contact Farmer
- Back to Buyer Dashboard

If reservation status = pending:
- Show Cancel Request button.
- Use existing cancelBuyerReservation helper.
- After cancel, update UI to cancelled.

Do not allow buyer to confirm, complete, or delete reservation.

4. Pickup/directions link

Add a simple “Open pickup location” button only if pickup_location exists.

Important:
- Use a plain free external map search URL only.
- Do not use Google Maps API.
- Do not use Mapbox.
- Do not embed a map.
- Do not require API keys.
- If this is risky or not clean, skip it and only show pickup location text.

5. Update Buyer Dashboard cards

On each Buyer Dashboard reservation card, add:

- View Details button linking to /buyer/reservations/:id

Keep:
- Contact Farmer
- View Listing
- Cancel Request for pending reservations
- Filters
- Search
- Empty states

6. Improve Buyer Dashboard sorting

Add simple sort dropdown:

- Newest first
- Oldest first
- Quantity high to low
- Status

Keep sorting client-side only.
No paid services.

7. Empty/loading/error states

Reservation Detail page should handle:
- Loading
- Not found
- Access denied / unavailable
- Supabase error

Use simple user-friendly wording.

8. Privacy/security

Verify:
- Buyer sees only own reservation details.
- Buyer cannot see another buyer’s reservation detail.
- Guest cannot access reservation detail.
- Buyer can cancel only pending own reservation.
- Buyer cannot update buyer_name, buyer_phone, listing_id, quantity_kg, payment_method, or created_at.
- Farmer Dashboard still sees own listing reservations.
- Admin Dashboard still sees all reservations.
- Buyer phone is not public.
- No secrets printed.
- No RLS weakening.
- No paid services used.

9. Testing

Use fake/mock data only.

Test:
- /buyer/reservations/:id is protected.
- Buyer can open own reservation detail.
- Buyer cannot open another buyer’s reservation detail.
- Invalid reservation id shows not-found state.
- View Listing works.
- View Farmer Profile works.
- Contact Farmer works.
- WhatsApp/tel/copy still work.
- Cancel pending request from detail page works.
- Cancel button disappears after cancellation.
- Buyer Dashboard View Details links work.
- Buyer Dashboard sorting works.
- Buyer Dashboard filters/search still work.
- Guest reservation flow still works.
- Logged-in buyer reservation still saves buyer_user_id.
- Farmer Dashboard still works.
- Admin Dashboard still works.
- Browse Produce still works.
- Produce Detail still works.
- Farmer Profile still works.
- Waitlist form still works.
- PWA files still exist.
- TypeScript has zero errors.
- No console errors.
- No secrets printed.
- No paid services used.

10. Documentation

Update README with:
- Buyer reservation detail route
- Buyer-only reservation visibility
- Cancel pending reservation behavior
- Pickup location/directions behavior
- Dashboard sort behavior
- Privacy rules
- Fake/mock testing rule
- No paid services used

After completion, report:
- Files changed
- Route added
- Supabase helper added/updated
- Whether buyer reservation detail loads
- Whether access control works
- Whether cancellation from detail works
- Whether dashboard View Details links work
- Whether sorting works
- TypeScript result
- Console errors, if any
- Remaining limitations