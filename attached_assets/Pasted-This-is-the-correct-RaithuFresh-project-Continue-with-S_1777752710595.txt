This is the correct RaithuFresh project.

Continue with Supabase backend setup for RaithuFresh only.

Project reminder:
RaithuFresh is a Telangana-first fruits and vegetables marketplace connecting local farmers, nearby buyers, assisted farmers, agents, and admins.

Important:
Do not use any DealNest files.
Do not create deals, coupons, stores, categories, comments, votes, threads, or deal alerts.
Do not add map.
Do not add authentication yet.
Do not add new features.
Do not rebuild the UI.
Only connect and verify Supabase database setup.

Step 1 — Confirm project identity

First inspect the project and confirm this is RaithuFresh by checking for:
- Landing page
- Browse Produce page
- Produce Detail page
- Farmer Dashboard
- Agent Dashboard
- Admin Dashboard
- RaithuFresh mock produce/farmer data

If this is not RaithuFresh, stop and tell me.

Step 2 — Verify secrets

Verify these secrets with Boolean output only:

- Boolean(process.env.VITE_SUPABASE_URL)
- Boolean(process.env.VITE_SUPABASE_ANON_KEY)
- Boolean(process.env.SUPABASE_DB_URL)

Do not print actual secret values.
Do not print the database URL.
Do not print the password.
Do not expose SUPABASE_DB_URL to frontend code.
Do not use any VITE_ variable for database migrations.

If any secret is false, stop and tell me exactly which one is missing.

Step 3 — Check Supabase files

Check if these files exist:

- supabase/schema.sql
- supabase/seed.sql

If they exist, inspect them and confirm they are RaithuFresh-only.

They should only create/use these tables:

- waitlist_leads
- farmers
- produce_listings
- reservations
- agent_call_requests

If schema.sql or seed.sql is missing, create them.

RaithuFresh database requirements:

1. waitlist_leads
Columns:
- id UUID primary key
- name text not null
- phone text not null
- role text check role in buyer/farmer/agent
- town text
- created_at timestamp default now()

2. farmers
Columns:
- id UUID primary key
- name text not null
- phone text
- village text
- district text
- rating numeric default 0
- verified boolean default false
- assisted_mode boolean default false
- created_at timestamp default now()

3. produce_listings
Columns:
- id UUID primary key
- farmer_id UUID references farmers(id)
- produce_name text not null
- category text check category in Fruit/Vegetable
- quantity_kg numeric not null
- price_per_kg numeric not null
- harvest_datetime timestamp
- pickup_location text
- district text
- distance_km numeric
- quality_notes text
- status text check status in active/reserved/sold/out_of_stock
- created_at timestamp default now()
- updated_at timestamp default now()

4. reservations
Columns:
- id UUID primary key
- listing_id UUID references produce_listings(id)
- buyer_name text not null
- buyer_phone text not null
- quantity_kg numeric not null
- status text check status in pending/confirmed/cancelled/completed
- payment_method text default 'Cash or UPI directly to farmer'
- created_at timestamp default now()

5. agent_call_requests
Columns:
- id UUID primary key
- farmer_name text not null
- farmer_phone text not null
- village text
- request_note text
- status text check status in pending/called/resolved
- created_at timestamp default now()

RLS:
Enable Row Level Security on all tables.

MVP public policies:
- Public insert into waitlist_leads
- Public read from active produce_listings
- Public insert into reservations
- Public read from verified farmers
- Keep update/delete restricted for now
- Add SQL comments that security must be tightened after real auth is added

Seed data:
Use realistic Telangana sample data:
- Farmers from Shadnagar, Vikarabad, Warangal, Karimnagar, Siddipet, Nalgonda
- Fruits: mango, banana, guava, papaya, watermelon
- Vegetables: tomato, brinjal, green chilli, okra, onion, cucumber
- Prices in ₹ per kg
- Quantities in kg
- Active statuses
- Verified farmers

Step 4 — Create safe DB setup script if missing

If not already available, create a safe Node script using pg.

Requirements:
- Use only process.env.SUPABASE_DB_URL
- Never print full DB URL
- Never print password
- Run supabase/schema.sql first
- Run supabase/seed.sql second
- Verify only RaithuFresh tables
- Stop on SQL errors with clear error message

Add package.json scripts:

- db:schema
- db:seed
- db:setup
- db:verify

Step 5 — Run setup

Run:

npm run db:setup

Then run:

npm run db:verify

Verify only these tables:

- waitlist_leads
- farmers
- produce_listings
- reservations
- agent_call_requests

Step 6 — Test frontend Supabase connection

Test:

1. Browse Produce loads Supabase produce listings.
2. Search and filters still work.
3. Waitlist form saves into waitlist_leads.
4. Produce Detail reservation form saves into reservations.
5. Farmer Dashboard can create a produce listing in produce_listings.
6. Admin Dashboard shows real Supabase counts.
7. If Supabase fails, mock fallback still works.
8. PWA support still remains.
9. No console errors.

Step 7 — Report

After completion, report:

- Whether this project was confirmed as RaithuFresh
- Whether all 3 secrets were true
- Files created/changed
- SQL files run
- Tables created/verified
- Which frontend forms now save to Supabase
- Whether Browse Produce loads real database listings
- Any errors or limitations
- Exact commands run

Do not add anything else.