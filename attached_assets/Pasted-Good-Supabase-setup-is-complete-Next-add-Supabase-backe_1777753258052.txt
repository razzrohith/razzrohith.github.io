Good. Supabase setup is complete.

Next, add Supabase-backed Agent Call Request functionality for RaithuFresh.

Important:
Do not add map.
Do not add authentication.
Do not add delivery.
Do not add payments.
Do not change the main UI design.
Do not rebuild the app.
Do not create new database tables unless absolutely required.
Do not touch DealNest-style data.
Keep project focused only on fruits and vegetables.

Goal:
Make the Agent Dashboard save farmer assistance/callback requests into the existing Supabase table:

agent_call_requests

Current table fields:
- id
- farmer_name
- farmer_phone
- village
- request_note
- status
- created_at

Required frontend changes:

1. Agent Dashboard — Callback / Assistance Request Form

Add or connect a form with these fields:
- Farmer name
- Farmer phone
- Village / town
- Request note
- Status default: pending

Submit behavior:
- If Supabase is configured, insert into agent_call_requests
- If Supabase fails, show a clear friendly error
- If Supabase is not configured, keep mock/local fallback behavior
- Show success message after save:
  "Agent callback request saved successfully."

Validation:
- Farmer name required
- Farmer phone required
- Village optional
- Request note optional
- Phone should not be empty

2. Agent Dashboard — Request List

Show recent agent call requests from Supabase.

Display:
- Farmer name
- Phone
- Village
- Request note
- Status
- Created date

If there are no requests, show:
"No callback requests yet."

3. Status update

Allow agent/admin to update request status:
- pending
- called
- resolved

Important:
Because current RLS may restrict updates, first test whether update works.
If update is blocked by RLS, do not weaken security blindly.
Instead, report the exact RLS issue and suggest the minimum safe policy needed for MVP.

4. Keep existing Agent Dashboard features

Do not remove:
- Assigned farmer list
- Stock update tool
- Call log notes
- Stock update history placeholder
- Commission tracking placeholder

5. Testing

Test:
- Agent callback request saves to Supabase
- Request appears in Agent Dashboard list
- Empty form validation works
- Status update works or reports RLS limitation clearly
- Existing Browse Produce still loads 15 Supabase listings
- Waitlist form still works
- Reservation form still works
- Farmer Dashboard listing save still works
- Admin Dashboard counts still work
- Mock fallback still works
- No console errors

6. Documentation

Update README with:
- What agent_call_requests does
- How to test the Agent Dashboard callback request
- Current MVP security limitation if status update is blocked by RLS

After completion, report:
- Files changed
- Whether callback request insert works
- Whether request list loads from Supabase
- Whether status update works
- Any RLS issue found
- Exact tests run
- Any limitations