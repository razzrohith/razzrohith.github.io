Do not add the map yet.

Next, add Supabase backend support for RaithuFresh.

Goal:
Convert the current RaithuFresh MVP from mock-only data to a Supabase-ready app where waitlist users, produce listings, and reservations can be saved in a real database.

Important:
Do not rebuild the app.
Do not change the visual design unless needed.
Do not remove existing PWA support.
Do not remove existing mock data.
Do not add map functionality yet.
Do not add delivery features.
Do not add grains, dairy, meat, grocery, restaurant, or ecommerce marketplace features.
Keep the app focused only on fruits and vegetables.

Use Supabase safely:
- Use VITE_SUPABASE_URL
- Use VITE_SUPABASE_ANON_KEY
- Read these from environment variables
- Do not hardcode Supabase keys
- Do not use service role key in frontend
- Do not commit secrets
- If Supabase env variables are missing, app should still work using mock data

Create Supabase client:
- Add src/lib/supabase.ts
- Add helper functions for checking whether Supabase is configured

Database SQL:
Create a file called supabase/schema.sql with tables for:

1. waitlist_leads
Columns:
- id UUID primary key
- name text not null
- phone text not null
- role text check role in buyer/farmer/agent
- town text
- created_at timestamp

2. farmers
Columns:
- id UUID primary key
- name text not null
- phone text
- village text
- district text
- rating numeric default 0
- verified boolean default false
- assisted_mode boolean default false
- created_at timestamp

3. produce_listings
Columns:
- id UUID primary key
- farmer_id UUID references farmers(id)
- produce_name text not null
- category text check category in Fruit/Vegetable
- quantity_kg numeric not null
- price_per_kg numeric not null
- harvest_datetime timestamp
- pickup_location text
- district text
- distance_km numeric
- quality_notes text
- status text check status in active/reserved/sold/out_of_stock
- created_at timestamp
- updated_at timestamp

4. reservations
Columns:
- id UUID primary key
- listing_id UUID references produce_listings(id)
- buyer_name text not null
- buyer_phone text not null
- quantity_kg numeric not null
- status text check status in pending/confirmed/cancelled/completed
- payment_method text default 'Cash or UPI directly to farmer'
- created_at timestamp

5. agent_call_requests
Columns:
- id UUID primary key
- farmer_name text not null
- farmer_phone text not null
- village text
- request_note text
- status text check status in pending/called/resolved
- created_at timestamp

RLS:
Enable Row Level Security on all tables.

For this MVP:
- Allow public insert into waitlist_leads
- Allow public read from active produce_listings
- Allow public insert into reservations
- Allow public read from verified farmers
- Keep update/delete restricted for now
- Add comments in SQL explaining this is MVP-level security and must be tightened when real auth is added

Frontend integration:

1. Waitlist form:
- Submit to Supabase if configured
- Show success message after save
- If Supabase is not configured, save locally or show mock success without crashing

2. Browse Produce:
- Load listings from Supabase if configured
- Join farmer info if possible
- If no Supabase data exists, show existing mock listings
- Preserve search and filters

3. Produce Detail:
- Load selected listing from Supabase or mock data
- Reservation form should insert into reservations table if Supabase is configured
- If not configured, keep current mock confirmation

4. Farmer Dashboard:
- Keep current form working
- If Supabase is configured, save new listing to produce_listings
- If no farmer exists, use a sample/default farmer for MVP testing
- Keep local/mock fallback

5. Admin Dashboard:
- If Supabase configured, show real counts from:
  - waitlist_leads
  - farmers
  - produce_listings
  - reservations
- If not configured, keep mock analytics

Seed data:
Create supabase/seed.sql with realistic Telangana farmer and produce data:
- Farmers from Shadnagar, Vikarabad, Warangal, Karimnagar, Siddipet, Nalgonda
- Fruits: mango, banana, guava, papaya, watermelon
- Vegetables: tomato, brinjal, green chilli, okra, onion, cucumber
- Prices in ₹ per kg
- Quantities in kg
- Active listing statuses

Documentation:
Update README with:
- How to create a Supabase project
- Which environment variables to add in Replit Secrets
- How to run schema.sql
- How to run seed.sql
- How to test waitlist save
- How to test reservation save
- How mock fallback works

Testing:
After implementation, test:
- App starts without Supabase env variables
- App starts with Supabase env variables
- Waitlist form works
- Browse page still works
- Filters still work
- Produce detail page works
- Reservation flow works
- Farmer listing form works
- Admin analytics do not crash
- PWA files are still present
- No console errors

Expected final output:
- Supabase-ready RaithuFresh MVP
- Real waitlist save
- Real reservation save
- Real produce listing save
- Mock fallback still working
- SQL schema and seed files included
- No secrets exposed
- No broken pages

After completion, report:
- Files created/changed
- Database tables created in SQL
- Environment variables needed
- Features connected to Supabase
- Features still using mock fallback
- How to test
- Any errors or limitations